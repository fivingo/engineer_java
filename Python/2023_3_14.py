# 2023년 3회 14번
# split 함수 | 실행 결과

x, y = input("x, y의 값을 공백으로 구분하여 입력: ")._____()
# split(sep=None, maxsplit=-1) | sep이 지정안되면 공백으로 처리

print("x의 값:", int(x))
print("y의 값:", int(y))

# 실행결과
# x, y의 값을 공백으로 구분하여 입력: 5 10
# x의 값: 5
# y의 값: 10
